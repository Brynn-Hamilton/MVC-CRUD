@model Teammember;
@{
    ViewData["Title"] = "Registered";
}
<div class="home">
    <h1>Registration Confirmation</h1>
<hr />

Thank you for registering for the potluck, @Model.FirstName!
<br />
We look forward to seeing you @if(@Model.GuestName != null && @Model.GuestName.Length > 0) { <span> and @Model.GuestName</span>} on @Model.AttendanceDate!
<br />
We will contact you at @Model.EmailAddress with any updates.
</div>

